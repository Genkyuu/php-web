version: '3.8'

services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - php_files:/var/www/html

  php:
    image: php:7.4-fpm
    volumes:
      - php_files:/var/www/html

volumes:
  php_files:
